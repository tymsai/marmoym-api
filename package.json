{
  "name": "tymsai-api",
  "version": "0.0.1",
  "description": "tymsai-api",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./lib/*",
    "copy-config": "mkdir ./lib/config && cp ./src/config/* ./lib/config/",
    "dev": "npm run clean && npm run copy-config && npm run watch",
    "watch": "tsc -w -p ./internals/typescript/tsconfig.dev.json",
    "build": "",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^7.0.14",
    "@types/sequelize": "^4.0.51"
  },
  "dependencies": {
    "express": "^4.15.2",
    "mysql": "^2.13.0",
    "sequelize": "^3.30.4"
  }
}
